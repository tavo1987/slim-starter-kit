
{% extends 'layout.twig'%}

{% block title %}Home{% endblock %}

{% block content %}
    <section>
        <p>
            Choose an option and delete the option that does not suit you.
        </p>

        <h2>Example form without vue.js</h2>
        <div class="form">
            <form action="{{ path_for('leads.store') }}" method="POST" id="lead">
                {{ csrf.field | raw }}
                <label>
                    Names <br>
                    <span>{{ errors.name | first }}</span>
                    <input type="text" name="name" value="{{ old.name }}">
                </label>

                <label>
                    Cédula <br>
                    <span>{{ errors.cedula | first }}</span>
                    <input type="number" name="cedula" value="{{ old.cedula }}">
                </label>

                <label>
                    Email <br>
                    <span>{{ errors.email | first }}</span>
                    <input type="text" name="email" value="{{ old.email }}">
                </label>
                <button class="button primary" type="submit">Enviar</button>
            </form>
        </div>

        <h2>Example form with Vuejs</h2>
        <app-form></app-form>
    </section>
{% endblock %}
